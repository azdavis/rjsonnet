{
  "`local` exp": {
    "prefix": "local",
    "body": ["local ${1:var} = ${2:exp};", "${3:exp}"]
  },
  "`if` exp": {
    "prefix": "if",
    "body": ["if ${1:exp} then", "  ${2:exp}", "else", "  ${3:exp}"]
  }
}
